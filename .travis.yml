#Team 7A Meal Planner
#Automated Testing using travis-ci

language: node-js
cache: npm

install:
  - npm install
  #not currently needed
  #- npm run setup
  #these are steps used to setup the web programming cw

script:
  - npm start
  #per package.json: npm server.js

deploy:
  provider: pages
  # github pages
  github-token: $GITHUB_TOKEN
  on:
    branch: testing
